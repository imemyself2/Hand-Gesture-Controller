# Hand-Gesture-Controller

This is a simple hand action recognizer which uses a cnn network to identify whether the palm of our hand is open or not. Based on that it drives the character in game.The model accuracy is 85%

This mini-project uses:

OpenCV
TensorFlow
Python
